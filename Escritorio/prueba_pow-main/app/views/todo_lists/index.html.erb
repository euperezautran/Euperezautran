<p id="notice"><%= notice %></p>

<div class="container">
  <div class="card card-index">
    <h1>Listas de Tareas</h1>

    <table>
      <thead>
        <tr>
          <th>Titulo</th>
          <th>Descripción</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @todo_lists.each do |todo_list| %>
          <tr>
            <td><%= todo_list.title %></td>
            <td><%= todo_list.description %></td>
            <td><%= link_to 'Ver', todo_list, class: "btn btn-primary" %></td>
            <td><%= link_to 'Editar', edit_todo_list_path(todo_list), class:"btn btn-secondary" %></td>
            <td><%= link_to 'Borrar', todo_list, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger"  %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Nueva lista de tareas', new_todo_list_path %>
    </div>
</div>
